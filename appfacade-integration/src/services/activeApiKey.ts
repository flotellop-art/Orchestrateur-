/**
 * STUB DE VALIDATION ISOLÉE — NE PAS COPIER DANS APPFACADE.
 *
 * Ce fichier existe uniquement pour permettre à `tsc --noEmit` de valider
 * `OrchestratorSync.tsx` en dehors du projet Appfacade. Dans le vrai projet
 * Appfacade, remplacez cet import par la fonction réelle qui retourne la
 * clé Anthropic active (ex. un getter depuis zustand, localStorage, etc.).
 *
 * Signature attendue par OrchestratorSync :
 *   getActiveApiKey(): string | null
 */

export function getActiveApiKey(): string | null {
  return null;
}
